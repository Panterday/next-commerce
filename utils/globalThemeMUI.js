import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#49bcbe',
            light: '#80eff1',
            dark: '#008b8e'
        },
        secondary: {
            main: '#fafafa',
            light: '#ffffff',
            dark: '#c7c7c7'
        }
    }
})

export default theme; 