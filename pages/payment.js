import Layout from "../components/layout/layout";
import Typography from "@mui/material/Typography";
const Payment = () => {
  return (
    <Layout>
      <Typography variant="h1" color="initial" mt="1em">
        PAGOS
      </Typography>
    </Layout>
  );
};

export default Payment;
